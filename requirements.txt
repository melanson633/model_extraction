# Core dependencies for AM Model Extraction


# Data processing and analysis
pandas>=1.3.0
numpy>=1.21.0
openpyxl>=3.0.9  # For Excel file handling

# Additional utilities that might be useful for financial analysis
xlrd>=2.0.1  # For reading older Excel formats
xlsxwriter>=3.0.0  # For writing Excel files with formatting

# Logging and utilities (built-in modules used: os, logging)
# These are part of Python standard library, no additional packages needed 
